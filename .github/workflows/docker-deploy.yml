name: Build and Push Docker Image to Aliyun ACR (Personal Instance)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  REGISTRY: crpi-ppzaquo8yzy4dm4g.cn-hangzhou.personal.cr.aliyuncs.com
  IMAGE_NAME: ai-travel

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Log in to Aliyun Personal ACR
      uses: docker/login-action@v3
      with:
        registry: crpi-ppzaquo8yzy4dm4g.cn-hangzhou.personal.cr.aliyuncs.com
        username: ${{ secrets.ALIBABA_CLOUD_USERNAME }}
        password: ${{ secrets.ALIBABA_CLOUD_PASSWORD }}
        
    - name: Extract metadata (tags & labels)
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: crpi-ppzaquo8yzy4dm4g.cn-hangzhou.personal.cr.aliyuncs.com/${{ secrets.ALIBABA_CLOUD_NAMESPACE }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}
          
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        tags: |
            crpi-ppzaquo8yzyd4m4g.cn-hangzhou.personal.cr.aliyuncs.com/jjzdnsh/ai-travel:latest
        build-args: |
          NEXT_PUBLIC_SUPABASE_URL=${{ secrets.SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}
